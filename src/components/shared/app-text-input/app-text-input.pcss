@keyframes fadeIn {
  0% {
    opacity: 0;
  }

  100% {
    opacity: 1;
  }
}

app-text-input {
  display: block;
  position: relative;
  padding-top: 16px;
  border-bottom: 1px solid var(--brightGray);
  margin-bottom: 25px;
  box-sizing: border-box;
  padding-bottom: 5px;

  &:after {
    content: ' ';
    display: block;
    position: absolute;
    bottom: -1px;
    left: 50%;
    transform: translate(-50%, 0);
    max-width: 0;
    width: 100%;
    height: 2px;
    overflow: hidden;
    transition: max-width var(--animationDuration) var(--animationTiming);
    background-color: var(--brightGreen);
  }

  & .label {
    position: absolute;
    top: 0;
    transition: all var(--animationDuration) var(--animationTiming);
    transform: scale(1) translate(0, 15px);
    transform-origin: left top 0;
    pointer-events: none;
    user-select: none;
    font-size: 16px;
    color: var(--textInputLabelText);
  }

  & .input {
    padding: 0;
    width: 100%;
    position: relative;
    border: none;
    outline: none;
    background-color: transparent;
    color: var(--textColor);
    cursor: inherit;
    height: 100%;
    box-sizing: border-box;
    font-size: 16px;
  }

  & .message-container {
    color: var(--textInputLabelText);
    transition: opacity var(--animationDuration) var(--animationTiming);
    font-size: 12px;
    transform: translateY(20px);
    position: absolute;
    left: 0;
    bottom: 0;
    animation: var(--animationDuration) fadeIn var(--animationTiming) forwards;
  }

  &.active {
    &:after {
      max-width: 100%;
    }

    & .label {
      transform: scale(0.75);
      color: var(--brightGreen);
    }

    & .message-container {
      color: var(--brightGreen);
    }
  }

  &.error {
    border-color: var(--red);

    &:after {
      background-color: var(--red);
    }

    & .label {
      color: var(--red);
    }

    & .message-container {
      color: var(--red);
    }
  }
}
